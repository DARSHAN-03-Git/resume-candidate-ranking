# Development and offline training/export dependencies
-r requirements.txt

# PyTorch (CPU) and Hugging Face training tools
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=3.0.0
optimum[onnxruntime]>=1.20.0

# Offline NER training (scripts/phase4_train_ner.py)
datasets>=2.14.0
seqeval>=1.2.2
