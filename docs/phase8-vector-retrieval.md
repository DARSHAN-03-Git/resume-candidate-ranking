# Phase 8: Vector Retrieval

The ranking core exposes vector-compatible candidate text and similarity functions. FAISS is the intended educational retrieval layer, followed by persistent Weaviate in Docker. Those services are not represented as running until their containers and indexes are actually verified.
