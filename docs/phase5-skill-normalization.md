# Phase 5: Skill Normalization and Inference

Aliases such as `K8s` normalize to `Kubernetes`. Inference rules add separate records with a basis, such as `FastAPI + Python` producing `Python Web Services`. Inferred skills are never mixed silently with explicit evidence.

Neo4j integration remains a persistence adapter for these relationships; the deterministic rules are the verified low-resource baseline.
