# Phase 9: Cross-Encoder Reranking

Candidate retrieval and final scoring are separate interfaces so a cross-encoder can rerank a small shortlist using the JD and candidate text together. No latency or cross-encoder metric is claimed before the model is installed and measured.
