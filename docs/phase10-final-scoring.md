# Phase 10: Final Scoring

The verified default score is `0.5 * semantic + 0.3 * experience + 0.2 * skills_jaccard`. Component values and weights are returned with every result. Experience is estimated only from parsed year ranges, so missing or ambiguous dates reduce confidence rather than becoming invented tenure.
